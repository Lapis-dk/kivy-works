BoxLayout:
    #cols : 1
    orientation : "vertical"
    padding : 15  #between gridlayouts and buttons
    spacing : 5
    #pos_hint : {'center_y' : 0.73}
    #adaptive_height : True
    GridLayout:

        cols:2
        spacing : 5  #between buttons and textfields etc
        # size_hint : (1,None)
        # height : '400dp'
        #pos_hint : { "right":1,"top":0.1 }
        Label:
            text:"Instruction Id"
            color:0,0,0,1
            text_size: self.size  # we have to bound it so that the text can be aligned to the left side
            halign: 'left'

        Label:
            text:"Admin Id"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'



        TextInput:
            multiline : False
            text : "UDI1456327"

        TextInput:
            multiline : False
            text : "UDI1456327"


        Label:
            text:"Student Id"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'

        Label:
            text:"Course"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'


        TextInput:
            multiline : False

        TextInput:
            multiline : False


        Label:
            text:"Stream"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'


        Label:
            text:"Total fee"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'


        TextInput:
            multiline : False

        TextInput:
            multiline : False


        Label:
            text:"Calender Year"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'

        Label:
            text:"concession"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'


        TextInput:
            multiline : False

        TextInput:
            multiline : False

    BoxLayout:
        orientation : "vertical"
        # size_hint : (1,None)
        # height : '150dp'
        Label:
            text:"Reason"
            color:0,0,0,1
            text_size: self.size
            halign: 'left'
            # size_hint : (1,.1)

    #       background_color: (0, 0, 1, 1) # to change the label color
    #        canvas.before:
    #            Color:
    #                rgba: self.background_color
    #            Rectangle:
    #                size: self.size
    #                pos: self.pos

        TextInput:
            multiline : False
            # size_hint : (1,.2)
            text : "type..."
        Spinner:
            text: "Select reason"
            values: ["Reason 1", "Reason 2", "Reason 3"]
        Label: